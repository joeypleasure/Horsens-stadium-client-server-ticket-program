
import java.io.Serializable;
import java.rmi.RemoteException;
import java.sql.SQLException;

public class LastTicketSoldTest implements Serializable
{

   public static void main(String[] args) throws SQLException, RemoteException
   {

      // TicketDbInterface db = new TicketDbAdapter();
      // System.out
      // .println("Last Sold Ticket was No: " + db.getLastSoldTicket());

      // ClientModelManager proc = new ClientModelManager();
      // System.out.println(proc.getAllSoldTickets());
      // System.out.println("Total Income: " + db.getTotalIncome());

      // System.out.println(db.cancelPurchase(1));

   }

}
